# Mewture Button Firmware

Firware that talks to the host via the DDAA (Ding Ding Ack Ack) protocol to a host to set and recieve the mute status of the host.

Tested on a Wemos D1 Mini (Clone with USB C).
